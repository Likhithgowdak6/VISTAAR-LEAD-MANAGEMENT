fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0

langgraph==0.2.60
langgraph-checkpoint-postgres==2.0.9
psycopg[binary]==3.2.3
psycopg-pool==3.2.4

openai==1.59.6
# 0.125.0, not 1.x: the 1.x SDK dropped `temperature` from messages.create in favour of
# output_config.effort, and this service sets temperature deliberately per call type (0.2 for a
# summary, 0.6 for a follow-up). 0.125.0 is the last release that accepts both, and it knows the
# current model IDs. Revisit when moving to output_config is worth the change in behaviour.
anthropic==0.125.0

docxtpl==0.19.0
python-docx==1.1.2

httpx==0.28.1
